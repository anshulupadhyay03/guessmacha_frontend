pluginManagement { includeBuild("../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }

def nodeExecutable = "node"
if (System.getProperty("os.name").toLowerCase().contains("mac")) {
    nodeExecutable = ["/usr/local/bin/node", "/opt/homebrew/bin/node"].find { new File(it).exists() } ?: "node"
}

extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> 
    ex.autolinkLibrariesFromCommand([nodeExecutable, "node_modules/react-native/cli.js", "config"]) 
}
rootProject.name = 'GuessMate'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
